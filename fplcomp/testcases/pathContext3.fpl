# a function that has a non-void return type must return a value

fun main(a:Int)->Int
    if a =1
        return 4
    else
        return 5
    # no error as all paths have a return

@-------------------
